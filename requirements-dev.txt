# Local development: the FastAPI server (app/main.py) and test helpers.
# Not bundled into Lambdas.
-r requirements.txt
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
pillow>=10.0
